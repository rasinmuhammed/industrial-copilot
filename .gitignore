.venv/
__pycache__/
*.pyc
data/warehouse.duckdb
evals/reports/*
!evals/reports/latest.json
.DS_Store
data/exemplars.jsonl

# Local run artifacts. `results/` reached 4.3 GB and `data/` 2.7 GB, which is
# what made `git add -A` hang: both are regenerated by `make build` and the
# eval harness, so neither belongs in history.
results/
data/results/
data/cmms.db
data/cmms.db-wal
data/cmms.db-shm
data/cmms.duckdb
