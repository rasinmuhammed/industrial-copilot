[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m copilot.ingest && uvicorn copilot.api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 90
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
