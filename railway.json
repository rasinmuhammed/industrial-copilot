{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "/opt/venv/bin/uvicorn copilot.api:app --host 0.0.0.0 --port $PORT --timeout-graceful-shutdown 5"
  }
}
